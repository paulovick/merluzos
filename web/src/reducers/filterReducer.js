
const INITIAL_STATE = {
  fromText: '',
  toText: '',
  fromPosition: null,
  toPosition: null
}

const filterReducer = (state = INITIAL_STATE, action) => {
  // TODO
  return state
}

export { filterReducer }